kind = "web"
previewPath = "/"
title = "احتفال أميرة وعلاء"
version = "1.0.0"
id = "artifacts/amira-alaa"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19742

[services.development]
run = "pnpm --filter @workspace/amira-alaa run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/amira-alaa", "run", "build" ]
publicDir = "artifacts/amira-alaa/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19742"
BASE_PATH = "/"
